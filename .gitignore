# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.next
out
test-results
playwright-report
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.claude/
# Codex(コードエージェント)がローカルに置く設定。リポジトリでは共有しない
.codex/
*.tsbuildinfo

# scripts/build-sprites.mjs が焼くスプライトシート。生成物なのでソースには入れない
# (このフォルダ配下は全て生成物。手で置いたファイルは次のビルドで消える)
/public/sprites/

# 使い捨ての検証用スクリプトとスクリーンショットの置き場(リポジトリ直下のみ)。
# .claude/ 配下の同名フォルダは上の .claude/ で既に外れている
/probe/
/tmp/
